#1
'class is constructor is blueprint for creating objects'
#2
'class methods is a funsctions that belong in a class'
'it also make behavior of objects created for class'
#3
class student():
    def __init__(self, name, grade, age, major, present, credit):
        self.name = name
        self.grade = grade 
        self.age = age 
        self.major = major 
        self.present = present
        self.credit = credit

    def attedence(self):
        if self.present == False:
            print(self.name + 'is absent')
        else:
            print(self.name + 'is present')

    def GrudautionCheck(self):
        if credits < 18:
            credit= 18 . credits
            print(self.)

#4
class VGC():
    def __init__(self, name, health, moveList, equip, ammo, ability):
        self.name = name #string
        self.health = health #integer 
        self.moveList = moveList #List
        self.equip = equip
        self.ammo = ammo
        self.ability = ability
        # MOVESET, WEAPONS, ABILTTIES, ARCHETYPE 

        def triggerAbility(self):
            if self.ability == 'static':
                print()
            elif self.ability == 'lighting rod':
                print()

        def typeAd
            

    

        