# Sql more structred = more searchable and linable with other pieces of data.
# best for data that needs to organized

# NoSQL less structred = less rules best for large amounts of data.
# where organization isn't a priority or possibitly. 